import { MapView, MyLocationButton, SearchBar } from "../components"

export const Home = () => {
  return (
    <div>
        <MapView />
        <MyLocationButton />
        <SearchBar />
    </div>
  )
}
